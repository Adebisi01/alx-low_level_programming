#include "main.h"
#include <stdio.h>

/**
 * _isupper  - check the code.
 *
 * Return: Always 0
 */
int _isupper(int c)
{
	if (int isupper(c) == 1)
	{
		return (1);
	}
	else if (int isupper(c) == 0)
	{
		return (0);
	}
}
