#include "main.h"

/**
 * _islower - checks lower characters
 * @c: character
 * Return: 1 or 0
 */
int _islower(int c)
{
	int i = 0;

	for (i = 97; i <= 122; i++)
	{
		if ('c' == i)
		{
			return (1);
		}
		else
			return (0);

	}
}
